# ============================================
# Next.js Environment Variables
# ============================================
# This file contains environment variables for your Next.js application.
# DO NOT commit this file to version control (it's already in .gitignore)

# ============================================
# SMTP Email Configuration
# ============================================
# Required for contact form and quote form submissions
# These settings are configured for GoDaddy SMTP by default

# SMTP Server Host
# Default: smtpout.secureserver.net (GoDaddy)
# For other providers, use:
# - Gmail: smtp.gmail.com
# - Outlook: smtp-mail.outlook.com
# - SendGrid: smtp.sendgrid.net
SMTP_HOST=smtpout.secureserver.net

# SMTP Server Port
# Default: 465 (SSL/TLS)
# Alternative ports: 587 (STARTTLS), 25 (not recommended)
SMTP_PORT=465

# SMTP Username (Your email address)
SMTP_USER=support@notustechnologies.co.in

# SMTP Password (Your email password or app-specific password)
SMTP_PASSWORD=Notus@1990@@@

# From Email Address
SMTP_FROM_EMAIL=support@notustechnologies.co.in

# Recipient Email Address
RECIPIENT_EMAIL=support@notustechnologies.co.in

# ============================================
# Next.js Public Variables
# ============================================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Use these for client-side configuration that doesn't need to be secret

# Example: Site URL (uncomment and set if needed)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Example: Google Maps API Key (if using Google Maps)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ============================================
# Development/Production Settings
# ============================================
# NODE_ENV is automatically set by Next.js
# Development: NODE_ENV=development
# Production: NODE_ENV=production
